{{ config(materialized='view') }}

SELECT
    SALESORDERID,
    REVISIONNUMBER,
    TRY_CAST(ORDERDATE AS TIMESTAMP) AS order_date,
    TRY_CAST(DUEDATE AS TIMESTAMP) AS due_date,
    TRY_CAST(SHIPDATE AS TIMESTAMP) AS ship_date,
    STATUS,
    ONLINEORDERFLAG,
    SALESORDERNUMBER,
    PURCHASEORDERNUMBER,
    ACCOUNTNUMBER,
    CUSTOMERID,
    SALESPERSONID,
    TERRITORYID,
    BILLTOADDRESSID,
    SHIPTOADDRESSID,
    SHIPMETHODID,
    CREDITCARDID,
    CREDITCARDAPPROVALCODE,
    CURRENCYRATEID,
    SUBTOTAL,
    TAXAMT,
    FREIGHT,
    TOTALDUE,
    COMMENT,
    ROWGUID,
    TRY_CAST(MODIFIEDDATE AS TIMESTAMP) AS modified_ts,
    ROW_HASH,
    PK_HASH,
    EFFECTIVE_FROM,
    EFFECTIVE_TO,
    IS_CURRENT,
    IS_DELETED,
    LOAD_TS
FROM {{ source('raw_fact', 'sales_salesorderheader_history') }}
