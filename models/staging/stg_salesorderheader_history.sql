{{ config(materialized='view') }}

SELECT
    SALESORDERID,
    REVISIONNUMBER,
    TO_TIMESTAMP(ORDERDATE, 'DD-Mon-YY HH:MI:SS AM') AS order_date,
    TO_TIMESTAMP(DUEDATE, 'DD-Mon-YY HH:MI:SS AM') AS due_date,
    TO_TIMESTAMP(SHIPDATE, 'DD-Mon-YY HH:MI:SS AM') AS ship_date,
    STATUS,
    ONLINEORDERFLAG,
    SALESORDERNUMBER,
    PURCHASEORDERNUMBER,
    ACCOUNTNUMBER,
    CUSTOMERID,
    SALESPERSONID,
    TERRITORYID,
    BILLTOADDRESSID,
    SHIPTOADDRESSID,
    SHIPMETHODID,
    CREDITCARDID,
    CREDITCARDAPPROVALCODE,
    CURRENCYRATEID,
    SUBTOTAL,
    TAXAMT,
    FREIGHT,
    TOTALDUE,
    COMMENT,
    ROWGUID,
    TO_TIMESTAMP(MODIFIEDDATE, 'DD-Mon-YY HH:MI:SS AM') as modified_ts,
    ROW_HASH,
    PK_HASH,
    EFFECTIVE_FROM,
    EFFECTIVE_TO,
    IS_CURRENT,
    IS_DELETED,
    LOAD_TS
FROM {{ source('raw_fact', 'sales_salesorderheader_history') }}
WHERE IS_CURRENT = TRUE