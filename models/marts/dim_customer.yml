models:
  - name: dim_customer
    columns:
      - name: customer_id
        tests:
          - not_null
          - unique
      - name: personid
        tests:
          - relationships:
              to: ref('dim_person')
              field: customer_id
