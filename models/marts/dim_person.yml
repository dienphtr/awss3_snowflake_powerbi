version: 2

models:
  - name: dim_person
    description: "Dimension khách hàng từ PERSON_PERSON_HISTORY."
    columns:
      - name: customer_id
        tests:
          - not_null
          - unique
